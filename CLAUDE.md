# 프로젝트 개발 지침 (Claude Code 작업 규칙)

이 문서는 Claude Code(개발 에이전트)가 이 프로젝트에서 작업할 때
반드시 지켜야 하는 규칙입니다.

## 프로젝트 목표

이 프로젝트의 궁극적인 목표는 ChatGPT가 총괄 두뇌(전체 작업을 설계하고
판단하는 역할) 역할을 하고, 필요에 따라 Claude 및 다른 AI를 전문가로
호출하며, Claude Code 같은 개발 에이전트(코드를 실제로 작성/수정하는
AI)가 승인된 작업을 실행하는 AI 협력 개발 프로그램을 만드는 것입니다.

## 핵심 작업 원칙

1. 사용자가 명확히 요청하지 않은 기능을 임의로 개발하지 않는다.
2. 조사 요청에서는 파일을 수정하지 않는다.
3. 계획 요청에서는 코드 구현을 시작하지 않는다.
4. 다음 작업은 사용자의 명시적인 승인 없이 수행하지 않는다.
   - 파일 대량 삭제
   - 기존 핵심 코드 대규모 재작성
   - 시스템 패키지 설치
   - 외부 서비스 가입 또는 설정 변경
   - API Key(에이피아이 키) 변경
   - Git Commit(깃 변경 저장)
   - Git Push(깃 원격 업로드)
   - Branch Merge(브랜치 병합)
   - main 브랜치 직접 수정
5. 코드 수정 전 현재 Git 상태를 확인한다.
6. 중요한 수정 전에는 복구 가능한 상태인지 확인한다.
7. 한 번에 하나의 작은 기능만 구현한다.
8. 기능 구현 후 테스트하고 결과를 보고한 뒤 멈춘다.
9. 실패한 테스트를 숨기거나 성공한 것처럼 보고하지 않는다.
10. 사용자는 프로그래밍 초보자이므로 보고는 쉬운 한국어로 한다.
    영어 개발 용어가 필요하면 바로 옆에 괄호로 한국어 뜻을 붙인다.
11. 기존에 정상 작동하는 기능을 새로운 기능 때문에 불필요하게
    다시 작성하지 않는다.
12. 요청 내용이 모호하거나 여러 구현 방법 중 선택이 중요한 경우,
    코드를 먼저 작성하지 말고 선택지를 설명한다.
13. 다른 자동 규칙이나 Hook(자동 실행 규칙)이 사용자의 명시적인
    지시와 충돌할 경우 사용자의 지시를 우선해야 하며, 충돌 사실을
    사용자에게 보고한다.
14. 이번 프로젝트의 궁극적인 목표는 ChatGPT가 총괄 두뇌 역할을 하고,
    필요에 따라 Claude 및 다른 AI를 전문가로 호출하며, Claude Code
    같은 개발 에이전트가 승인된 작업을 실행하는 AI 협력 개발
    프로그램을 만드는 것이다.

## 1인용 내부 도구 원칙

AI Development Studio는 판매용 제품이나 SaaS(온라인 구독 서비스)가
아니라, 사용자 본인 1명만 사용하는 개인용 내부 도구입니다.
따라서 다음 기능은 별도 요청이 없는 한 만들지 않습니다.

- 회원가입 / 로그인
- 사용자 계정, 다중 사용자 관리
- 구독 결제
- 사용자별 권한 관리
- 서버 기반 회원 시스템
